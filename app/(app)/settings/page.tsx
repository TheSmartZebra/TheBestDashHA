import { PlaceholderPage } from "../../../components/layout/PlaceholderPage";

export default function SettingsIndexPage() {
  return <PlaceholderPage title="Settings" phase="later phases" note="Use the ⋯ menu to jump to a specific section." />;
}
